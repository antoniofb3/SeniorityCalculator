<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_registration"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.antoniofb.senioritycalculator.registration.RegistrationActivity">

    <EditText
        android:id="@+id/employeeName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Employee name"
        android:imeOptions="actionNext"
        android:inputType="textPersonName"
        android:lines="1"
        android:maxLines="1"
        android:layout_centerHorizontal="true" />

    <EditText
        android:id="@+id/employeeJob"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Employee job"
        android:imeOptions="actionNext"
        android:inputType="textCapSentences"
        android:lines="1"
        android:maxLines="1"
        android:layout_below="@id/employeeName"/>

    <EditText
        android:id="@+id/employeeSeniority"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Employee seniority"
        android:imeOptions="actionDone"
        android:inputType="textCapSentences"
        android:lines="1"
        android:maxLines="1"
        android:layout_below="@id/employeeJob"/>

    <Button
        android:id="@+id/btnRegisterEmployee"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Register employee"
        android:layout_below="@id/employeeSeniority"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>
